# dotnet-data-processing-api
dotnet-data-processing-api is a .NET Core Web API designed to ingest, validate, and process structured data using RESTful endpoints. The application persists data into SQL Server using Entity Framework Core and follows a clean, layered architecture suitable for enterprise-scale backend systems.
